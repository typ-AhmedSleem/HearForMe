package com.typ.hearforme.designsystem.theme

import androidx.compose.ui.graphics.Color

// Brand Colors
val PrimaryBlue = Color(0xFF4D4DFF)
val PrimaryLight = Color(0xFF7A7AFF)
val PrimaryDark = Color(0xFF3333CC)

// Alert Colors
val AlertHigh = Color(0xFFFF4D6D)      // Pinkish Red (Baby Cry)
val AlertNormal = Color(0xFFFF9F1C)    // Orange (Doorbell)
val AlertCritical = Color(0xFFE63946)  // Red (Siren)
val AlertInfo = Color(0xFF48CAE4)      // Cyan (Running Water)
val AlertWarning = Color(0xFFFFD166)   // Yellow (Dog Bark)

// Neutral Colors
val BackgroundLight = Color(0xFFF8F9FF)
val SurfaceLight = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF0D1B2A)
val OnSurface = Color(0xFF1B263B)
val TextSecondary = Color(0xFF778DA9)

// Gradients
val PrimaryGradient = listOf(PrimaryBlue, Color(0xFF70D6FF))
